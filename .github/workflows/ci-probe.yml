name: Visitor Light CI probe

on:
  pull_request:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm GitHub Actions is running
        run: echo "GitHub Actions is active for Website_Visitor_Light"
